// Placeholder chat page (Task 7). The streaming chat UI — session list, SSE token rendering,
// citations, refusal-supersede, the five async states, and feedback — is built in Task 8.

export function ChatPage() {
  return (
    <div className="placeholder">
      <h1>Chat</h1>
      <p>The grounded chat experience is coming next.</p>
    </div>
  )
}
